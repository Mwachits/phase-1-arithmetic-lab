// multiply: an equation whose multiplied results will equal 62
const num1 = 31;
const num2 = 2;
const multiply = num1 * num2;

// random: generates a random integer greater than 0
const random = Math.floor(Math.random() * 100) + 1;

// mod: an equation that calculates a remainder that is equal to 4
const num3 = 14;
const num4 = 10;
const mod = num3 % num4;

// max: finds the highest number in a set; the value returned should be 20
const max = Math.max(5, 10, 20, 15);

// Exporting variables if needed (for Node.js or module systems)
// module.exports = { num1, num2, num3, num4, multiply, random, mod, max };
